import { LocaltoutcPipe } from './localtoutc.pipe';

describe('LocaltoutcPipe', () => {
  it('create an instance', () => {
    const pipe = new LocaltoutcPipe();
    expect(pipe).toBeTruthy();
  });
});
